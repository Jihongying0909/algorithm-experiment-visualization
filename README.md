# Algorithm Experiment Visualization

算法可视化实验的统一导航页，包含 `test2-demo` 至 `test6-demo` 的项目入口。

## GitHub Pages

仓库创建并推送后，在仓库的 **Settings > Pages** 中选择 **Deploy from a branch**，然后选择 `main` 分支和 `/ (root)` 目录。

发布地址将是：

`https://jihongying0909.github.io/algorithm-experiment-visualization/`
